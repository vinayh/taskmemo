[project]
name = "taskmemo"
version = "0.1.0"
description = "Daily task printout for a Phomemo M02 Pro thermal printer."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bleak>=3.0.1",
    "pillow>=12.2.0",
]
